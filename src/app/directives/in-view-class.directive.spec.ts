import { InViewClassDirective } from './in-view-class.directive';

describe('InViewClassDirective', () => {
  it('should create an instance', () => {
    const directive = new InViewClassDirective();
    expect(directive).toBeTruthy();
  });
});
